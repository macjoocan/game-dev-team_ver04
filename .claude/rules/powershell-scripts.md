---
paths:
  - "scripts/**"
---

# PowerShell 스크립트는 ASCII 전용

`.ps1` 파일에 한글 등 비ASCII 문자열/주석을 넣지 마라.
PS 5.1은 BOM 없는 .ps1을 ANSI로 파싱해서, UTF-8 한글 바이트가 따옴표(0x22)를
멀티바이트 문자 일부로 삼키며 **에러 없이 로직이 변형**된다(루프 무시 등 실제 사례 있음).
사용자 메시지가 한글이어야 하면 스크립트 밖(호출부·문서)에서 처리한다.
