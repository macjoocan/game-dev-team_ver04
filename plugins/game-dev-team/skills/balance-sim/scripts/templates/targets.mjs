// targets.mjs - 게임성 성공 지표. **기획이 소유한다.**
//
// 이 값은 개발자가 정하는 게 아니다. 기획 게이트(1->2)에서 숫자로 정의된 목표를 여기 옮긴다.
// 각 항목에 **기획 문서의 경로와 줄**을 주석으로 붙여라 - 리포트의 [기준] 칸이 그걸 가리킨다.
// 출처가 없는 목표는 근거 없는 추정값이고, 기획 게이트에서 이미 반려됐어야 한다.
//
// 정책을 붙여서 정의해야 한다: "휴리스틱 봇 기준 35~55%" 처럼. 봇 승률 != 사람 승률이다.

export const TARGETS = {
  // 출처: docs/GDD.md L?? — TODO 실제 경로·줄로 바꿔라
  winRate: [0.35, 0.55],      // 휴리스틱 봇 기준
  lengthMean: [8, 15],        // 런 길이 (턴·웨이브 - 단위는 프로젝트가 정한다)
  choicesMean: [3, 12],       // 판당 의미 있는 선택 수
  maxPickShare: 0.35,         // 최다 선택지 픽률 상한 (초과 = 지배 전략)

  // ── 난이도 곡선 판정용 (curve-check.mjs) ──────────────────────────────────
  //
  // **stageOrder 를 채워라.** 곡선 판정은 진행 순서를 알아야 성립한다. 비워두면
  // 이름 자연순으로 추론하고, 실제 순서와 다르면 "역전 구간" 판정이 전부 허위로 나온다.
  stageOrder: [],             // 예: ['stage1', 'stage2', ..., 'boss1']

  clearBand: [0.35, 0.75],    // [좌절선, 후반 지루함선] — 조건부 통과율 기준
  clearSpike: 0.25,           // 인접 스테이지 간 허용 낙차
  clearExceptions: [],        // 의도된 예외로 등재한 스테이지 (예: 회피 퍼즐 보스)
};
